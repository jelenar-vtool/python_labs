import argparse
import os

def parse_doc_reg(file_path):
#TODO: Write function that does explaind 
    
    return defines_lines

def write_defines_file(defines_lines, output_dir):
    #TODO: Create output file path, and write to it
 

if __name__ == "__main__":
    # TODO: Create an argument parser object

    # Parse doc_reg.txt and get defines lines
    defines_lines = parse_doc_reg(args.file_path)

    if defines_lines:
        # TODO: Write the defines lines to defines.txt
     
    else:
         # Print a message if no valid lines were found
        print("No valid lines found in doc_reg.txt.")

